const frutas: string[] = ['Banana', 'Maçã', 'Laranja'];

// frutas.push(1); não são aceitos
// frutas = [1];

const numeros: Array<number> = [1, 2, 3, 4, 5];

console.log(frutas);
console.log(numeros);

let obj = {
  nome: 'Giselle',
  idade: 33
}

console.log(obj);
console.log(obj.nome);